input:
  file:
  - file: stdin
    index: stdin
  webservice:
  - port: "0000"
    address: 0.0.0.0
    index: default
processing:
  regex:
  - regex: ^(this)*
    mapping: (1) thing
output:
  file:
  - file: stdout
  webservice:
  - port: "0000"
    address: 0.0.0.0