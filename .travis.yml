language: go
sudo: false
go:
- 1.6

os:
 - linux
 - osx
 - windows

script:
  - go test -v ./... -coverprofile=coverage.txt -covermode=atomic

after_success:
  - bash <(curl -s https://codecov.io/bash)

